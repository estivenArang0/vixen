server:
  port: 8086

spring:
  application:
    name: shipping-service
  data:
    mongodb:
      host: ${MONGODB_HOST:localhost}
      port: 27017
      database: vixen_shipping
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379

logging:
  level:
    com.vixen: INFO
