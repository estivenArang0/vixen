server:
  port: 8084

spring:
  application:
    name: cart-service
  data:
    mongodb:
      host: ${MONGODB_HOST:localhost}
      port: 27017
      database: vixen_carts
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379

logging:
  level:
    com.vixen: INFO
